apiVersion: 1

datasources:
  - name: SQLite
    uid: efneymi371atca
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true

    jsonData:
      path: /opt/machineinnovation/db/metrics.db